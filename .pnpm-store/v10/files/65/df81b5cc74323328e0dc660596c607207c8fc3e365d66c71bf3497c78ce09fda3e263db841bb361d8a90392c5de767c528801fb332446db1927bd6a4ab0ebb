import type { ParseSource } from '../parse/source.js';
/**
 * Parse link destination: returns { ok, pos, str }
 */
export declare function parseLinkDestination(str: ParseSource, start: number, max: number): {
    ok: boolean;
    pos: number;
    str: string;
};
export default parseLinkDestination;
