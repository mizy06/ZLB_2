/**
 * Inline rule: text
 * Skip text characters for text token, place those to pending buffer
 * and increment current pos
 */
export declare function text(state: any, silent?: boolean): boolean;
export default text;
