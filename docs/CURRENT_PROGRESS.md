# 当前进度

更新时间：2026-07-22

## 总体状态

`experiment/bone` 已从串行知识图谱 Demo 升级为可运行的 C+ 课程思维导图 Agent。

当前主任务入口不再使用：

```text
chunk -> 模型一次生成节点和自由关系 -> 字符串归一
```

而是使用：

```text
证据账本
  -> 全局根与一级主题
  -> 递归分支团队
  -> 全局合并与覆盖审计
  -> Top-k 父候选
  -> 独立校验 / 仲裁
  -> CP-SAT 合法主树
  -> 人工复核
  -> 图版本
```

## 已完成

- LangGraph Main Supervisor。
- 可复用、可递归、并发执行的 Branch Team 子图。
- 文本与视觉 Content Unit Ledger。
- 根候选、一级主题和结构节点。
- 显式节点抽取与确定性阶段降级。
- PPTX/PDF 页面渲染、原生视觉提取、VLM bbox 分析与裁剪。
- `standalone_node`、`attach_as_media`、`decompose`、`ignore_decoration` 视觉决策。
- 同义归一、分支归属、视觉媒体融合和覆盖补建。
- Top-k 父候选召回。
- 标准档独立校验。
- 高精档第二校验与分歧仲裁路径。
- OR-Tools CP-SAT 唯一根、唯一父、深度和抽象节点约束。
- NetworkX 无环、连通、根可达与证据硬校验。
- 主树与跨链分离。
- SQLite 共享黑板。
- 内容单元、节点候选、父边候选、跨链候选和决策记录持久化。
- 阶段检查点和服务重启后的最新图恢复。
- 人工保留、删除、改父、改名和版本化。
- 任务删除时同步删除黑板记录和视觉资产。
- React 树形工作台、跨链开关、证据侧栏、质量门、节点表和复核队列。
- 桌面端与移动端响应式布局。
- 百炼外部引擎接口和低代码蓝图兼容。
- Kimi Provider 与 `kimi-k3` 单服务商工作台。
- age 密文启动时内存解密为当前后端进程的 `KIMI_API_KEY`。
- Kimi 生成、校验、视觉和仲裁独立角色调用。
- SQLite 历史记录列表、恢复和删除入口。
- 完整 JSON 与高清 PNG 本地导出。
- 前后端同源生产镜像与持久化 Compose 部署。

## 验证记录

- Python 单元测试：9 项通过。
- C+ 本地启发式端到端：通过。
- FastAPI 上传、轮询、版本读取、任务删除：通过。
- 示例课件结果：
  - 15 个节点。
  - 14 条主树边。
  - 拓扑合法。
  - 最终节点证据覆盖率 100%。
  - 重要内容加权覆盖率 100%。
- PPTX 原生图片提取：通过。
- 视觉 bbox 裁剪：通过。
- 临时保底边强制复核：通过。
- 人工复核产生新图版本：通过。
- React/TypeScript 生产构建：通过。
- 历史记录 API 与服务重启恢复：通过。
- 桌面端与移动端历史抽屉：通过。
- JSON/PNG 浏览器下载：通过。

## 外部部署项

以下工作依赖账号或部署环境，不属于仓库内代码缺口：

- 在目标环境提供 Kimi 密文与对应的本机 age 私钥。
- 确认 Kimi 账号具有 `kimi-k3` 模型权限。
- 为视觉资产配置公网 HTTPS `ASSET_PUBLIC_BASE_URL`。
- 使用开发集、校准集和盲测集校准业务阈值。

## 凭据处理

以下内容不会提交到 Git：

- `RAM.txt`
- `ZLB-apiKey-*.csv`
- `*apiKey*.csv`
- `.env`
- `kimi.local.env`
- `.secrets/`
- `*.age.pub`
- `*-identity.txt`
- `.data/`
- Python 虚拟环境
- 前端依赖和构建产物

仓库可保留 age 密文，但本机私钥、公钥和明文 ENV 不进入 Git。Provider Key 仅在后端进程内存在，不进入前端、提示词、日志、决策记录或图版本。
